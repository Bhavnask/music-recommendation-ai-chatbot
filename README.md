# Music Recommendation AI Chatbot

Simple web app that recommends songs by mood and language using a Python backend and a lightweight front-end.

## 🚀 Features
- Pick mood (happy / neutral / sad) + language (English / Hindi / Kannada)
- Returns curated MP3s from local folders
- Clean HTML/CSS/JS front-end

## 🗂 Project Structure

## ⚙️ Setup

### 1) Python env
```bash
python -m venv .venv
. .venv/Scripts/activate  # Windows
pip install -r requirements.txt
python main.py
